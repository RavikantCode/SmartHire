import React from 'react';
import useGetData from '@/hooks/useGetData';

const TestComponent = () => {
    useGetData(); // Call your hook

    return (
        <div>
        </div>
    );
};

export default TestComponent;
